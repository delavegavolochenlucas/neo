# Variáveis sao como lugares que podemos guardar infos, para depois utilizarmos elas sem tem que escrever tudo de novo.

idade = 14
# variável int, que é de números inteiros (10, 11, 12, 13, 14, 15 e por aí vai)

altura = 1.65
# váriavel float, que é de números decimais (1.45, 1.55, 1.65, 1.75 e por aí vai)

nome = "Lucas"
# váriavel string, que é de texto (Lucas, Daniel, Desiree e por aí vai)

verdadeiro = True
falso = False
# variável bool, que é de condiçao (Verdadeiro ou Falso)

print(nome + " tem " + str(idade) + " anos")
# o str é para converter uma váriavel int para string, transformando em texto.
# e o print() serve para mostrar algo no terminal.